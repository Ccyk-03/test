# ============================================================
# 配置导入文件（apply_config.py 使用）
# 每行一个 key=value，# 开头为注释，空行忽略；只写需要更新的项即可
# 多行内容用 \n 表示换行
# ============================================================

# API Key（替换 config.json 里的 api_key；留空=不修改）
apikey=sk-xxxxxxxxxxxxxxxx

# 指令 s 系列（横屏通用版）
s1=第一行示例\n第二行示例
s2=第一行示例\n第二行示例
s3=第一行示例\n第二行示例

# 指令 g 系列（竖屏 9:16 版，OpenRouter 用）
g1=第一行示例\n第二行示例
g2=第一行示例\n第二行示例
g3=第一行示例\n第二行示例
